# WHAT COUNTS AS A PROGRAMMING LANGUAGE (PL)?

A programming language can be many things: a runtime, interpreter, compiler, or
transpiler. Compilers can take decades to write so nobody is
expecting the next python -- even getting a single contrived program to run is a
huge accomplishment.

